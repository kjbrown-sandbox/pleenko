# Godot 4+ specific ignores
.godot/
/android/

# Stray .import files in build output (Godot regenerates these when run
# from the build directory — they're not real project files).
builds/**/*.import

# GdUnit4 (dev dependency, installed via AssetLib)
addons/gdUnit4/
